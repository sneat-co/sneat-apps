import { NgModule } from '@angular/core';
import { BasicSelectorComponent } from '@sneat/components';

@NgModule({
	declarations: [
		BasicSelectorComponent,
	],
})
export class BasicSelectorModule {
}
