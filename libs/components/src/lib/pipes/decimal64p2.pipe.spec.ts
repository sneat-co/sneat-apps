import { Decimal64p2Pipe, Numeral2Pipe } from './decimal64p2.pipe';

describe('Decimal64p2Pipe', () => {
  it('should create', () => {
    expect(new Decimal64p2Pipe()).toBeTruthy();
  });
});

describe('Numeral2Pipe', () => {
  it('should create', () => {
    expect(new Numeral2Pipe()).toBeTruthy();
  });
});
