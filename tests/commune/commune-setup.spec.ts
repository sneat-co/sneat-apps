import { test } from '@playwright/test';

test.describe.skip('Space Setup', () => {
  // TODO: Port commune setup flow once the UI is stable.
  // The original Cypress spec was commented out due to broken assertions.

  test('should create a new space', async () => {
    // Placeholder for future implementation.
  });
});
